package com.qa.interfaces;

// is an interface rather than a class
public interface BirdInterface {
	
	//Interfaces are solely abstract and have no actual methods or attributes
	//But, any class can use as many interfaces as you want
	
	//the only datat this class can contain are abstract methods
	//becasue ALL methods MUST be abstract no need to call them abstract
	
	public void catchFish();
	public void eatFish();
	
}
